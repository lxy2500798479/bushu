# 敏感配置（生产环境建议使用 External Secrets 或 Vault）
# 注意：values 需要 base64 编码
# echo -n 'your-value' | base64
apiVersion: v1
kind: Secret
metadata:
  name: seckill-secret
  namespace: seckill
type: Opaque
stringData:
  # 使用 stringData 可以直接写明文，K8s 会自动编码
  DATABASE_URL: "mysql://root:123456@mysql-service:3306/flash_sale"
  RABBITMQ_URL: "amqp://admin:123456@rabbitmq-service:5672"
  REDIS_PASSWORD: ""
